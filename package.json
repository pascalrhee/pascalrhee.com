{
  "name": "pascalrhee-website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "smoke": "node scripts/smoke.mjs",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^7.2.2"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
